#:schema node_modules/wrangler/config-schema.json
name = "adopecanva"
pages_build_output_dir = "dist"
compatibility_date = "2024-04-03"

[vars]
# Add environment variables like GEMINI_API_KEY in the Cloudflare Dashboard 
# for security. They will be available in your code as process.env.VARIABLENAME 
# after being injected by Vite during build.
