# File to Markdown

Convert PDFs, DOCX, CSV, HTML, and more to clean Markdown.

Legacy location: `views/tools/FileToMarkdown.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
