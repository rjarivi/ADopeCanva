# EXIF Metadata Stripper

Inspect camera info, remove GPS geo-tags, and sanitize photos before sharing.

Legacy location: `views/tools/ExifStripper.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
