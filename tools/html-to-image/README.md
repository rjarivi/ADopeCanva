# HTML to Image

Write HTML & CSS in a live editor and export it as a PNG, JPEG, or WebP image.

Legacy location: `views/tools/HtmlToImage.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
