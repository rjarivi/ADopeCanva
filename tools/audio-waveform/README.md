# Audio Waveform SVG

Export your audio as a scalable SVG waveform — bars, sharp, or smooth with gradients and image masking.

Legacy location: `views/tools/AudioWaveformExporter.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
