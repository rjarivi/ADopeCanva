# Universal Converter

Convert between JSON, XML, CSV, and YAML.

Legacy location: `views/tools/UniversalConverter.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
