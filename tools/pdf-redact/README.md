# PDF Redactor

Draw black bars over sensitive text. Normal or permanent true-redact mode.

Legacy location: `views/tools/PdfRedact.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
