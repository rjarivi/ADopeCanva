# Text Cleaner

Remove repetitive phrases and clean formatting.

Legacy location: `views/tools/TextCleaner.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
