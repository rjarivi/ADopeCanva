# Favicon Generator

Generate a full favicon bundle — ICO, PNG variants, apple-touch-icon, android icons, and webmanifest.

Legacy location: `views/tools/ImageToIco.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
