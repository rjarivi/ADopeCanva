# Universal Doc Converter

Convert Word, Markdown, HTML, and Images to PDF/HTML.

Legacy location: `views/tools/UniversalDocConverter.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
