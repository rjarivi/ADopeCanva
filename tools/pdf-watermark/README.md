# PDF Watermark & Numberer

Add confidential watermarks, text stamps, and page numbers to PDFs.

Legacy location: `views/tools/PdfWatermark.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
