# Markdown Creator

Design UI mockups using ASCII and Unicode characters for AI prompt engineering.

Legacy location: `views/tools/MarkdownCreator.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
