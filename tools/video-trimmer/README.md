# Video Trimmer

Cut clips with frame precision and volume control.

Legacy location: `views/tools/VideoTrimmer.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
