# Palette Extractor

Extract dominant color palettes, moods, and CSS variables from images.

Legacy location: `views/tools/PaletteExtractor.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
