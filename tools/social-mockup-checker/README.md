# Social Mockup Checker

Preview Instagram Stories, Reels, Feed Posts, Carousels & Ads inside a 3D phone mockup with official Meta safe zone guides.

Legacy location: `views/tools/SocialMockupChecker.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
