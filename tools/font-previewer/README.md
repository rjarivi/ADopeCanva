# Font Previewer

Browse and preview 100+ Google Fonts with custom text and sizes.

Legacy location: `views/tools/FontPreviewer.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
