# Audio Replacer

Swap audio tracks in videos with volume mixing.

Legacy location: `views/tools/AudioReplacer.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
