# Audio Trimmer

Trim and cut audio with waveform visualization.

Legacy location: `views/tools/AudioTrimmer.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
