# Mockup Generator

Drop your screenshot into a cinematic device frame — iPhone, Android, iPad, MacBook, Browser, or Window.

Legacy location: `views/tools/DeviceMockup.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
