# Image Compressor

Reduce file size without losing visible quality.

Legacy location: `views/tools/ImageCompressor.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
