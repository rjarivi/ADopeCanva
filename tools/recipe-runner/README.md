# recipe-runner (Lane 1 runtime)

Executes `recipes/*.json` with trusted engines only (FFmpeg WASM, canvas).
No recipe ships code — the component is fixed; only data varies.

To add a preset, add JSON (see `recipes/README.md`), not code here.
