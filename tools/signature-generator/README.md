# Sign to Gif

Create animated signatures and export as GIF/MP4.

Legacy location: `views/tools/SignatureGenerator.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
