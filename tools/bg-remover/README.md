# Smart BG Remover

Instantly remove image backgrounds locally in your browser with zero server uploads.

Legacy location: `views/tools/BackgroundRemover.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
