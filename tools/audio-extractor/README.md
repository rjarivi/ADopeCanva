# Audio Extractor

Extract crystal-clear audio, music, and voice from any video file.

Legacy location: `views/tools/AudioExtractor.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
