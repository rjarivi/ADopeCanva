# Gradient Creator

Design linear, radial, and conic gradients and export as PNG.

Legacy location: `views/tools/GradientCreator.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
