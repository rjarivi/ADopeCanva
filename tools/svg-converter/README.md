# SVG Code to SVG

Paste SVG markup, preview it live, and download as an .svg file.

Legacy location: `views/tools/SvgConverter.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
