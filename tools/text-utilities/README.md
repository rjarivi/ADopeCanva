# Text Utility Suite

Word count, case converter, slug generator & more.

Legacy location: `views/tools/TextUtilities.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
