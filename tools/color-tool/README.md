# Color Tool

Generate color palettes, verify contrast ratio, scale tints/shades, and convert color formats.

Legacy location: `views/tools/ColorTool.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
