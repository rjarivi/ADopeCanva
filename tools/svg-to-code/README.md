# SVG to Code

Upload an SVG file and extract its raw source markup.

Legacy location: `views/tools/SvgToCode.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
