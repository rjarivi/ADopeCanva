# Image Resizer

Resize images with precise dimension control.

Legacy location: `views/tools/ImageResizer.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
