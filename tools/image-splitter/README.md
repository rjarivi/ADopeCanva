# Image Splitter

Split wide or tall images into equal slices for seamless Instagram carousels & stories.

Legacy location: `views/tools/ImageSplitter.tsx` (re-exported here).
To rebuild natively: replace `index.tsx` with a real component using
`<ToolShell>` + `useToolFile()` and keep `manifest.json` in sync.
